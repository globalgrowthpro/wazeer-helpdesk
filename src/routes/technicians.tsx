import { createFileRoute, Link } from "@tanstack/react-router";
import { CheckCircle2, Clock3, MapPin, UsersRound } from "lucide-react";
import { AppShell } from "@/components/helpdesk/app-shell";
import { Panel, SectionHeading, Stat } from "@/components/helpdesk/ui";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { technicians } from "@/lib/helpdesk-data";
export const Route = createFileRoute("/technicians")({ head: () => ({ meta: [{ title: "الفنيون | وزير الحلو" }, { name: "description", content: "متابعة الفنيين وأحمال العمل والمهام الحالية." }, { property: "og:title", content: "الفنيون | وزير الحلو" }, { property: "og:description", content: "متابعة الفنيين وأحمال العمل والمهام الحالية." }, { property: "og:type", content: "website" }, { name: "twitter:card", content: "summary" }] }), component: TechniciansPage });
function TechniciansPage() { return <AppShell title="الفنيون"><SectionHeading title="فريق الصيانة" description="تابع التوفر والتخصص والمهام المسندة لكل فني" /><section className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4"><Stat label="إجمالي الفنيين" value="28" icon={UsersRound} /><Stat label="المتاحون الآن" value="11" icon={CheckCircle2} /><Stat label="في مهمة" value="14" icon={Clock3} /><Stat label="زيارات اليوم" value="8" icon={MapPin} /></section><Panel title="قائمة الفنيين"><div className="grid gap-4 p-5 md:grid-cols-2 xl:grid-cols-3">{technicians.map((tech) => <Link key={tech.id} to="/technicians/$technicianId" params={{ technicianId: tech.id }} className="rounded-lg border border-border p-4 transition-colors hover:bg-surface"><div className="flex items-start justify-between"><div className="grid h-11 w-11 place-items-center rounded-full bg-flow font-bold text-brand-ink">{tech.name.slice(0, 1)}</div><Badge variant="outline">{tech.status}</Badge></div><h2 className="mt-4 font-bold">{tech.name}</h2><p className="mt-1 text-sm text-muted-foreground">{tech.skill} · {tech.zone}</p><div className="mt-4 flex items-center justify-between border-t border-border pt-3 text-xs"><span>{tech.load} مهام نشطة</span><span>{tech.completed} مكتملة</span></div></Link>)}</div></Panel></AppShell>; }