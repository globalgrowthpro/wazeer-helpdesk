import { createFileRoute, Link } from "@tanstack/react-router";
import { CheckCircle2, Clock3, ListChecks, UserRound } from "lucide-react";
import { AppShell } from "@/components/helpdesk/app-shell";
import { Panel, SectionHeading, Stat } from "@/components/helpdesk/ui";
import { Badge } from "@/components/ui/badge";
import { tickets } from "@/lib/helpdesk-data";
export const Route = createFileRoute("/tasks")({ head: () => ({ meta: [{ title: "المهام | وزير الحلو" }, { name: "description", content: "متابعة مهام الصيانة المسندة." }, { property: "og:title", content: "المهام | وزير الحلو" }, { property: "og:description", content: "متابعة مهام الصيانة المسندة." }, { property: "og:type", content: "website" }, { name: "twitter:card", content: "summary" }] }), component: TasksPage });
function TasksPage() { return <AppShell title="المهام"><SectionHeading title="مهام فريق الصيانة" description="المهام الحالية موزعة حسب الأولوية والفني" /><section className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4"><Stat label="قيد التنفيذ" value="14" icon={Clock3} /><Stat label="بانتظار القبول" value="5" icon={UserRound} /><Stat label="مكتملة اليوم" value="32" icon={CheckCircle2} /><Stat label="إجمالي المهام" value="51" icon={ListChecks} /></section><Panel title="المهام الحالية"><div className="grid gap-3 p-5 md:grid-cols-2 xl:grid-cols-3">{tickets.map((ticket) => <Link key={ticket.id} to="/tickets/$ticketId" params={{ ticketId: ticket.id }} className="rounded-lg border border-border p-4 hover:bg-surface"><div className="flex justify-between gap-2"><span className="text-xs font-bold text-brand-ink">{ticket.id}</span><Badge variant="outline">{ticket.status}</Badge></div><h2 className="mt-3 font-bold">{ticket.title}</h2><p className="mt-2 text-sm text-muted-foreground">{ticket.technician} · {ticket.branch}</p><div className="mt-4 border-t border-border pt-3 text-xs font-bold text-brand-red">متبقي {ticket.sla}</div></Link>)}</div></Panel></AppShell>; }