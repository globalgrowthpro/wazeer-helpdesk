import { createFileRoute, Link, notFound } from "@tanstack/react-router";
import { ArrowRight, Building2, CheckCircle2, Clock3, MapPin, Phone, Plus, RotateCcw, Star } from "lucide-react";
import { AppShell } from "@/components/helpdesk/app-shell";
import { Panel, Stat } from "@/components/helpdesk/ui";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { branches, tickets } from "@/lib/helpdesk-data";
export const Route = createFileRoute("/branches/$branchId")({ loader: ({ params }) => { const branch = branches.find((item) => item.id === params.branchId); if (!branch) throw notFound(); return branch; }, head: ({ loaderData }) => ({ meta: [{ title: loaderData ? `${loaderData.name} | وزير الحلو` : "الفرع غير موجود" }, { name: "description", content: "تفاصيل الفرع وبلاغاته وطلبات الدعم." }, { property: "og:title", content: loaderData ? `${loaderData.name} | وزير الحلو` : "الفرع غير موجود" }, { property: "og:description", content: "تفاصيل الفرع وبلاغاته وطلبات الدعم." }, { property: "og:type", content: "website" }, { name: "twitter:card", content: "summary" }] }), component: BranchDetail });
function BranchDetail() { const branch = Route.useLoaderData(); const branchTickets = tickets.filter((item) => item.branchId === branch.id); return <AppShell title="تفاصيل الفرع"><div><Button asChild variant="ghost" size="sm"><Link to="/branches"><ArrowRight className="h-4 w-4" />العودة للفروع</Link></Button><div className="mt-3 flex flex-col gap-3 sm:flex-row sm:items-end sm:justify-between"><div><h1 className="text-2xl font-bold">{branch.name}</h1><p className="mt-1 text-sm text-muted-foreground">مدير الفرع: {branch.manager}</p></div><Button asChild><Link to="/branches"><Plus className="h-4 w-4" />إنشاء بلاغ</Link></Button></div></div><section className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4"><Stat label="بلاغات مفتوحة" value={String(branch.open)} icon={Clock3} /><Stat label="بلاغات مغلقة" value={String(branch.closed)} icon={CheckCircle2} /><Stat label="رضا الفرع" value={`${branch.satisfaction}/5`} icon={Star} /><Stat label="الحالة" value="يعمل" icon={Building2} /></section><section className="grid gap-4 xl:grid-cols-[1.2fr_.8fr]"><Panel title="بلاغات الفرع"><div className="divide-y divide-border">{branchTickets.length ? branchTickets.map((ticket) => <Link key={ticket.id} to="/tickets/$ticketId" params={{ ticketId: ticket.id }} className="flex flex-col gap-2 p-5 transition-colors hover:bg-surface sm:flex-row sm:items-center sm:justify-between"><div><p className="text-xs font-bold text-brand-ink">{ticket.id}</p><p className="mt-1 font-bold">{ticket.title}</p></div><div className="flex items-center gap-3"><span className="font-mono text-sm text-brand-red">{ticket.sla}</span><Badge variant="outline">{ticket.status}</Badge></div></Link>) : <p className="p-5 text-sm text-muted-foreground">لا توجد بلاغات مفتوحة.</p>}</div><div className="flex flex-wrap gap-2 border-t border-border p-4"><Button><CheckCircle2 className="h-4 w-4" />تأكيد الحل</Button><Button variant="outline"><RotateCcw className="h-4 w-4" />إعادة فتح البلاغ</Button></div></Panel><Panel title="بيانات التواصل"><div className="space-y-4 p-5"><div className="flex gap-3"><Phone className="h-5 w-5 text-brand-ink" /><div><p className="text-xs text-muted-foreground">الهاتف</p><p className="mt-1 font-bold" dir="ltr">{branch.phone}</p></div></div><div className="flex gap-3"><MapPin className="h-5 w-5 text-brand-ink" /><div><p className="text-xs text-muted-foreground">العنوان</p><p className="mt-1 text-sm font-bold">{branch.address}</p></div></div></div></Panel></section></AppShell>; }